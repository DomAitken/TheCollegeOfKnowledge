docker swarm init		(makes the node run from the first manager node on DockerDesktop)
docker swarm init --advertise-addr=[ip address]

docker swarm join --token [token]		(copy command and run it in another node to join the swarm)
docker swarm join-token manager
